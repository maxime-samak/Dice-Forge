package game.card;

import game.BotScore;
import game.ScoreCounter;

public class BuyCard {

    /** public static boolean buyCard(NeutralCard card, BotScore botScore, ScoreCounter scoreCounter){
        int[] cost = card.getPrice(); //{ un int , Resource.SOLAR},{un int , Resource.LUNAR}}


        int solar_monney = botScore.getSolar();
        int lunar_monney = botScore.getLunar();

        if(solar_monney <= solar_cost && lunar_monney <= lunar_cost) {

            scoreCounter.deduceSolar(botScore, solar_cost);
            scoreCounter.deduceLunar(botScore, lunar_cost);
            scoreCounter.moreVictory(botScore, card.getVictory());
            return true;
        }
        else{
            return false;
        }

    }**/

}
